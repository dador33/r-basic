#Tener presente que se debe instalar Rtools la version recomendada.
install.packages("tidyverse",dep = TRUE)
library("tidyverse")

install.packages("magic", dep=TRUE)

library(magic)

magic(6)

installed.packages()

